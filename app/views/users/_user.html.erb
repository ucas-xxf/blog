
<li>
    <%= gravatar_for user, size: 50%>
    <% if current_user.admin? && !current_user?(user)%>
        <%= link_to "删除", user,method: :delete, data: {confirm: "You sure?"}%>
        <%= link_to user.name,showuser_user_path(user.id)%>
    <%else%>
        <%= link_to user.name,user%>
    <%end%>
</li>